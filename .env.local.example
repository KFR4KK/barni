# Copy this file to .env.local (which is gitignored) and fill in real
# values. Never commit .env.local. See docs/AUTH_SETUP.md for how to
# obtain each of these.

# --- Database -----------------------------------------------------------
# Full Postgres connection string. Local dev can point at a local Postgres
# instance, a free Neon/Supabase project, or Docker — anything Postgres.
DATABASE_URL="postgresql://user:password@localhost:5432/the_collective"

# --- Auth.js core ---------------------------------------------------------
# Auth.js signs/encrypts session cookies with this. Generate one with:
#   npx auth secret
# (or: openssl rand -base64 33)
# Must be set in every environment (local AND production) and must be a
# different value in production than in development.
AUTH_SECRET=""

# Required in production so Auth.js knows its own public URL when
# constructing the Discord redirect URI. Not required for local dev
# (defaults to http://localhost:3000).
# AUTH_URL="https://yourdomain.com"

# --- Discord OAuth --------------------------------------------------------
# From the Discord Developer Portal -> your application -> OAuth2.
# See docs/AUTH_SETUP.md for the full walkthrough.
AUTH_DISCORD_ID=""
AUTH_DISCORD_SECRET=""

# --- Discord Bot (server membership sync) ---------------------------------
# Separate credential from AUTH_DISCORD_ID/SECRET above: this is the bot
# token used server-side to check server membership, never sent to or
# usable by the browser. See docs/DISCORD_SETUP.md for the full walkthrough.
DISCORD_BOT_TOKEN=""
# The Discord server (guild) ID that membership is checked against.
DISCORD_GUILD_ID=""
# Invite link shown by the "Join Discord" button when a member isn't in
# the server yet. Create a permanent, non-expiring invite for this.
DISCORD_INVITE=""

# --- Google OAuth -----------------------------------------------------------
# From Google Cloud Console -> APIs & Services -> Credentials -> OAuth
# client ID (type: Web application). Authorized redirect URI must be
# exactly: http://localhost:3000/api/auth/callback/google (dev) and
# https://yourdomain.com/api/auth/callback/google (prod).
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# --- Telegram Login Widget ---------------------------------------------
# NOT OAuth2 — created via @BotFather in Telegram, not a developer
# console. Two things BotFather gives you:
#   1. A bot token (/newbot) — server-only secret, used to verify the
#      signed login payload (see lib/telegram-auth.ts).
TELEGRAM_BOT_TOKEN=""
#   2. The bot's own @username — safe to expose client-side (it's what
#      the login widget displays), and MUST be set with the NEXT_PUBLIC_
#      prefix so the browser can render the widget.
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=""
# Also required: run /setdomain in @BotFather and set it to your site's
# domain (no protocol, e.g. "barni-three.vercel.app") — the widget
# refuses to render on any other domain, and never renders at all on
# localhost by Telegram's own design.

# --- Supabase Storage (lib/storage.ts) -------------------------------------
# Your Supabase project's URL. It's fine if this is the REST endpoint
# (ending in /rest/v1/) — lib/storage.ts normalizes it — but the bare
# project URL ("https://xxxx.supabase.co") also works.
SUPABASE_URL=""
# Service role key — server-only, never expose this to the browser.
# Project Settings -> API -> service_role key.
SUPABASE_SERVICE_ROLE_KEY=""
# Name of the Storage bucket. Must exist already (create it in the
# Supabase dashboard) and must allow public read access, since uploaded
# image URLs are stored and rendered directly (see docs/AUTH_SETUP.md-style
# setup notes, or the chat response that introduced this module, for the
# bucket policy to apply).
SUPABASE_STORAGE_BUCKET="uploads"
